# Tank-10: UI Dropdown Filtering

**Date:** 2026-05-17T02:20:31Z  
**Mode:** background  
**Scope:** lightEffects UI dropdown filtered to favorites only; non-favorites still resolve by name; nextIndex no longer advances for non-favorites  

## Files touched
- `drivers/gemstone-lights/gemstone-lights.groovy`

## Outcome
`lightEffects` attribute now contains favorites only, reducing the UI dropdown visual clutter. Numeric `setEffect` addresses favorites; non-favorites remain accessible via `setEffect(String)` and `playEffectByName(String)` for backward compatibility. Index advancement logic prevents accidental non-favorite selection.
