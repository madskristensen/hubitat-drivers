# Tank [tank-6] — v0.1.12 (2026-05-17T15:28:00Z)

**Spawn:** Tank (background, claude-sonnet-4.6)  
**Task:** Convert numeric enum commands to NUMBER type to defeat Hubitat dropdown +1 quirk

## What shipped

- Converted `setFlameBrightness()` command from ENUM type to NUMBER type (values 1–5)
- Converted `setFlameColor()` command from ENUM type to NUMBER type (values 1–6)
- Converted `setLogColor()` command from ENUM type to NUMBER type (values 1–12)
- Removed ENUM labels that were causing Hubitat UI dropdown off-by-one index mapping

## Commits

- `9e4fe1e` — Touchstone v0.1.12: convert flame/log color + brightness to NUMBER to defeat dropdown quirk

## Status

✅ COMPLETE — v0.1.12 shipped with numeric commands (NUMBER type). Brightness change later reverted in v0.1.13; this was an intermediate step in the enum-type investigation.

## Notes

This version discovered that Hubitat's dropdown +1 platform bug required a workaround. Subsequent versions explored named ENUM labels as a better solution.
