# Orchestration Log: Cypher Framework Fingerprint Pass 2

**Timestamp:** 2026-05-16T22:30:14Z
**Agent:** Cypher
**Role:** Integration/Protocol Discovery
**Requested By:** Mads Kristensen
**Mode:** Background
**Duration:** ~2 hours (15:30 to 22:30 UTC equivalent)

---

## Task
Framework-fingerprint the Gemstone Lights Gen 2 controller HTTP server from verbose curl headers to identify framework stack (Express, Flask, Actix, custom, etc.).

---

## Approach

### Phase 1: Verbose Curl Analysis (Completed)
- Captured HTTP response headers from 192.168.1.238:80 with `-v` flag
- Analyzed: `Server:` header absence, `Connection: close`, error message format
- Cross-referenced against common framework signatures

### Phase 2: Web Search for Framework Patterns (Completed)
- Searched for exact string `"Invalid route."` across GitHub repos
- Found 100+ generic matches—no unique signature
- Conclusion: Generic 404 message across all major frameworks

---

## Findings

### Primary Observation
```
No Server: header present
{"error": "Invalid route."}
Connection: close
Content-Type: application/json
```

### Interpretation
- **Server header absence:** Strongest signal; suggests custom/minimal framework or intentional suppression
- **Error string:** Too generic to fingerprint
- **Connection handling:** Simple server, likely resource-constrained (embedded device)
- **JSON API:** API-first design, REST conventions likely

### Confidence: 60% Custom Lightweight Framework
- Most likely: Custom Node.js (Restana, find-my-way) or Go (net/http)
- Less likely: Rust (actix-web), Python (Flask/FastAPI with custom 404)
- Cannot uniquely identify framework from passive headers

---

## Deliverables

### 1. Comprehensive 12-Probe Runbook
Ordered by yield-per-effort:

**Phase 1: HTTP Method Variation (5 min)**
1. OPTIONS / — discover allowed methods
2. POST / — some servers respond only to non-GET
3. PUT / — REST convention check
4. HEAD / — test for hidden headers

**Phase 2: Common REST Paths (10 min)**
5. /api/v1/ — most likely pattern (Gemstone community)
6. /api/v1/status — Gemstone pattern
7. /lights — generic IoT endpoint
8. /info or /about — device metadata

**Phase 3: Framework Debug/Introspection (5 min)**
9. /_routes or /__routes__ — route list middleware
10. /admin or /debug — admin panel

**Phase 4: Alternate API Versioning (5 min)**
11. /v1/status — alternate convention
12. /rest/status — JAX-RS convention

### 2. Port Scan Directive
If port 80 exhausted: scan 3000, 3001, 5000, 5001, 8000, 8001, 8080, 8081, 8443, 8888, 9000, 9001, 9090, 81, 443

### 3. mitmproxy Gate
- **GO:** If all probes return 404 on all open ports → escalate to mitmproxy
- **SKIP:** If any probe returns 200/201/valid JSON → proceed with protocol mapping

---

## Files Written
- `.squad/decisions.md` — merged Framework Fingerprint decision into "Active Decisions"
- `.squad/decisions/inbox/cypher-framework-fingerprint.md` — DELETED (inbox consumed)

---

## Outcome
✓ Framework cannot be uniquely identified from HTTP headers alone
✓ Generic 404 error string confirmed across all frameworks
✓ 12-probe runbook delivered to user
✓ mitmproxy gated as fallback for live traffic capture
✓ Next action: Run Phase 1–4 probes; escalate if still 404

---

## Blockers / Unknowns
- No `Server:` header is intentional suppression or true absence—cannot disambiguate
- Framework fingerprinting exhausted; REST path enumeration now required
- mitmproxy depends on: cert pinning behavior, same VLAN/firewall, and local API existence (not cloud-only)

---

## Next Steps (User's Decision)
1. Execute probe sequence (Phase 1–4)
2. If all 404, run port scan
3. If all ports exhausted, escalate to mitmproxy
4. Return first non-404 response for protocol mapping
