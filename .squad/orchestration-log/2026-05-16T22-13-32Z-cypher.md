# Cypher Orchestration: Local API Discovery Playbook
**Date:** 2026-05-16T22:13:32Z
**Agent:** Cypher 🌐 (Integration / Protocol)
**Requested by:** Mads Kristensen
**Mode:** background

## Why Chosen
User needs either a local API capture (to unlock v0.2.0 local path) or confirmation that the local API doesn't exist (so v0.2.0 uses cloud API exclusively). Cypher produced a comprehensive Windows-friendly discovery runbook.

## Files Authorized to Read
- Gemstone API research from prior sessions
- Gemstone app behavior notes

## Files Produced
- `cypher-local-capture-playbook.md` (merged into .squad/decisions.md)

## Outcome
**Delivered:** Comprehensive five-technique Windows-friendly capture runbook for discovering the Gemstone local API. Sequence: port scan (PowerShell Test-NetConnection on common HTTP ports, 5 min) → curl probe of common firmware paths (5 min) → mDNS discovery (10 min) → Wireshark capture (30 min) → mitmproxy phone-as-proxy + CA cert install for HTTPS decryption (45 min, recommended if app uses TLS without pinning). Playbook includes prerequisites, step-by-step instructions, filter expressions, expected outputs, troubleshooting, and what to do with captures. Recommends starting with 5-min port scan + curl probe.

**Key finding:** Tank confirmed cloud API is fully specced in decisions.md. Both paths (cloud + local) are now viable for v0.2.0; user choice pending.
