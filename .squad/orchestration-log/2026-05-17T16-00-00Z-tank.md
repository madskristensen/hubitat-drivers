# Tank [tank-8] — v0.1.14 (2026-05-17T16:00:00Z)

**Spawn:** Tank (background, claude-sonnet-4.6)  
**Task:** Roll back flame + log color from invented labels to NUMBER; remove legacy setDpRaw alias

## What shipped

- Rolled back `setFlameColor()` from **invented ENUM labels** (Red/Orange/Yellow/Green/Blue/Purple) to NUMBER type (values 1–6)
- Rolled back `setLogColor()` from **invented ENUM labels** (Crimson/Coral/Gold/Sage/Azure/Lavender) to NUMBER type (values 1–12)
- Kept `setFlameBrightness()` as named ENUM with correct labels: Dimmest / Dim / Medium / Brighter / Brightest (intact from v0.1.13)
- Removed legacy `setDpRaw()` command alias per Trinity's review finding #9
- Kept `setRawDP()` as the canonical discovery/testing command

## Commits

- `00517b4` — Touchstone v0.1.14: rollback flame + log color to NUMBER (safe interim state)

## Status

✅ COMPLETE — v0.1.14 shipped as a safe interim state with numeric color commands (no invented labels) and brightness kept as correct ENUM.

## Notes

This version avoided the invented-labels error by going back to NUMBER type. Brightness labels were proven correct by user feedback, so they were kept as ENUM. Next version (v0.1.15) sourced authoritative flame color labels from Tuya app screenshot for proper restoration.
