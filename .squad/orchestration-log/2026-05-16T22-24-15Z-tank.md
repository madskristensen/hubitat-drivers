# Orchestration Event — 2026-05-16T22:24:15Z

**Source:** Tank (🔧 Driver Developer)
**Agent/Actor:** Tank
**Requested by:** Mads Kristensen (via Coordinator direction)
**Mode:** Ship (synchronous)
**Why:** v0.1.0 shipped silent — commands produced log output but no device interaction and zero indication to user. Tank implements UX transparency: a `log.warn` banner fires on every `sendCommand()` invocation, making the stub state unmistakable even with `logEnable=false`.

---

## Files Written
- `drivers/gemstone-lights/gemstone-lights.groovy` — added log.warn banner, bumped to v0.1.1
- `drivers/gemstone-lights/packageManifest.json` — version → "0.1.1"

---

## Outcome
**v0.1.1 shipped with scaffold transparency:**
- Every `sendCommand()` call produces an unconditional `log.warn`: `"[Gemstone] v0.1.1 SCAFFOLD — '[action]' was NOT sent to the device (local HTTP endpoint not yet wired). This is expected until v0.2.0."`
- Version bumped consistently across all four locations (file header, DRIVER_VERSION field, changelog, packageManifest).
- User now sees clear indication in Hubitat log that driver is still under development and commands are not reaching the device.
- Pending user action: re-import v0.1.1 into Hubitat IDE to verify banner appears.
