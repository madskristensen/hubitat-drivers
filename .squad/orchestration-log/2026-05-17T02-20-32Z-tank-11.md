# Tank-11: State Pruning & Hygiene

**Date:** 2026-05-17T02:20:32Z  
**Mode:** background  
**Scope:** state.effectCatalog and state.effectPatterns now favorites-only; non-favorite name lookups trigger on-demand refresh; pruneNonFavoriteStateEntries() added for upgrade migration  

## Files touched
- `drivers/gemstone-lights/gemstone-lights.groovy`

## Outcome
Persistent state cache now contains favorites only. Non-favorite name resolutions are on-demand and ephemeral—catalog is fetched, effect resolved, played, and discarded. `pruneNonFavoriteStateEntries()` called from `updated()` and `initialize()` to clean up existing installs on upgrade. No public API changes; all non-favorite names remain resolvable at runtime.
