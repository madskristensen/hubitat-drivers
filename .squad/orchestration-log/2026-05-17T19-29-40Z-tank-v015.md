# Orchestration Log — 2026-05-17T19:29:40Z — Tank v0.1.5

## Agent: Tank

## Task: Touchstone v0.1.5 — remove paragraph() from preferences

### Summary

Third Hubitat sandbox fix in the install-time bug series. `paragraph()` is an app-only preference UI construct and isn't allowed in drivers.

### Changes

- Removed the `paragraph` header from the `preferences {}` block in `drivers/touchstone-fireplace/touchstone-fireplace.groovy`
- Moved the power-on-defaults explanation into the affected `input` descriptions for `defaultFlameColor`, `defaultFlameBrightness`, `defaultLogColor`, and `defaultHeatingSetpoint`
- Bumped the driver header/version/user-agent stamp to `v0.1.5`
- Audited for other app-only constructs (`section`, `href`, `app`, `mode`, `pageDefault`) — none found

### Skill Update

Updated `.squad/skills/tuya-local-groovy/SKILL.md` with consolidated three-families framing:
1. Import allowlist
2. Reflection blocked
3. App-only preference UI

### Status

Ready for v0.1.5 install.
